<div class="container page-top">

  <div class="row">
      <div class="col-md-9 mx-auto">
        <h2 style="margin:20px;">ユーザー新規登録</h2>
          <p style="font-size:12px;">※入力項目は全て<span class="badge badge-danger">必須項目</span>となっております。登録完了後、紹介文やプロフィール画像の設定が可能となります。</p>
          <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <%= render "layouts/error_messages", model: @user %>


            <div class="field-registration">
              <%= f.label :名前（ニックネームでも可） %>
              <%= f.text_field :name, autofocus: true, autocomplete: "name", class:"form-control col-md-5", required: true %>
            </div>

            <div class="field-registration">
            <%= f.label :性別 %>
            <div class="radio-inline">
              <%= f.radio_button :gender_status, :male %>
              <%= f.label :男性, class: "gender_status_label", required: true %>

              <%= f.radio_button :gender_status, :female %>
              <%= f.label :女性, class: "gender_statuslabel", required: true %>
            </div>
            </div>

            <div class="field-registration">
              <%= f.label :生年月日 %><br>
              <%= f.date_select :birth_date, use_month_numbers: true,
              start_year: 1930, #入力値の開始位置（西暦）
              end_year: (Time.now.year - 5),
              include_blank: true, class:"form-control", required: true %>
            </div>

            <div class="field-registration">
              <%= f.label :都道府県 %><br>
              <%= f.select :prefecture, User.prefectures.keys, {prompt: '選択してください'}, class: 'form-control btn btn-info col-md-4', required: true %>
            </div>

            <div class="field-registration">
              <%= f.label :メールアドレス %>
              <%= f.email_field :email, autofocus: true, autocomplete: "email",class:"form-control col-md-6", required: true %>
            </div>

            <div class="field-registration">
              <%= f.label :パスワード（6文字以上） %>
              <% if @minimum_password_length %>
              <% end %>
              <%= f.password_field :password, autocomplete: "new-password",class:"form-control col-md-6", required: true %>
            </div>

            <div class="field-registration">
              <%= f.label :"パスワード（確認再入力）" %>
              <%= f.password_field :password_confirmation, autocomplete: "new-password",class:"form-control col-md-6", required: true %>
            </div>

            <div class="actions text-center">
              <%= f.submit "登録する", class:"btn btn-primary", style:"width:150px; margin-top:30px;" %>

            </div>
          <% end %>
      </div>
    </div>
</div>
