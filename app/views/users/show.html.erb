<div class="container">
<% if @user.id == current_user.id %>
    <h3>マイページ</h3>
<% else %>
    <h3>会員情報詳細</h3>
<% end %>

    <div class="row" style="margin-top:50px;">
        <div class="col-md-4 offset-2">
            <div class="profile-left">
                <%= attachment_image_tag(@user,:profile_image, fallback: "/assets/no-image.png", size:"150x150") %>
                <table class="table">
            <thead>
                <tr>
                <th scope="col">都道府県</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                <td><%= @user.prefecture%></td>
                </tr>
            </tbody>
                <thead>
                <tr>
                <br><th>自己紹介</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                <td><%= @user.introduction %></td>
                </tr>
            </tbody>
            </table>
            </div>
        </div>
        <div class="col-md-6">
            <div class="profile-right">
                <h4>登録名:<%= @user.name %></h4>
                <p><%= @user.birth_date %> (<%= @user.age %>)<%= @user.gender_status_i18n %></p>

                <% if @user.id == current_user.id %>
                <%= link_to " 編集する", edit_user_path, class:"btn btn-primary btn-sm glyphicon glyphicon-wrench" %>
                <% end %>
                <br><br><br><table class="table">
                <thead>
                <tr>
                <th></th>
                <th scope="col">所属チーム</th>
                </tr>
            </thead>
            <tbody>
                <% @teams.each do |team|%>
                <tr>
                <td><%= @user.team %></td>
                </tr>
                <% end %>
            </tbody>
            </table>
            </div>
        </div>
    </div>
</div>